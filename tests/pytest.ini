[pytest]
# Зачем этот файл: он делает rootdir = tests/. Без него pytest поднимается выше,
# видит __init__.py пакета ноды и импортирует его в Package.setup, а тот тянет
# torch/tiktoken, которых нет вне ComfyUI. Тесты валидатора от ComfyUI не зависят.
